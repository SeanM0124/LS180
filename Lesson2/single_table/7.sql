--Use SQL to determine the average, minimum, and maximum wingspan for the birds shown in the table.

SELECT max(wingspan) FROM birds; -- 73.5

SELECT min(wingspan) FROM birds; -- 24

SELECT avg(wingspan) FROM birds; -- 43.2